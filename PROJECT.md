# Файл состояния проекта — «Лапки Дома»

> **Единая точка входа.** В начале новой сессии достаточно прочитать этот файл —
> пересказывать прошлую переписку не нужно. Обновлять после каждого значимого шага.
>
> 📁 **Папка проекта:** `D:\Рабочий стол\lapki-doma`
>
> 🔗 **Второй проект в портфеле пользователя.** Первый и уже работающий —
> «Расти Дома» (растения), папка `D:\Рабочий стол\МОИ ПРОЕКТЫ\Расти Дома`
> (до 26.07.2026 называлась `zeleny-dom`; имя репозитория на GitHub осталось
> `Poli6976/zeleny-dom`), сайт rasti-doma.ru. «Лапки Дома» построены копированием
> и адаптацией того же
> движка (Astro → GitHub Pages → Яндекс РСЯ → партнёрки маркетплейсов). При
> сомнениях в устройстве кода можно свериться с оригиналом, но НЕ смешивать
> файлы/контент двух проектов.

**Дата последнего обновления: 2026-07-26**

---

## 1. Цель проекта

Русскоязычный контентный сайт про уход за кошками и собаками — второй источник
пассивного дохода, по тому же проверенному плейбуку, что и «Расти Дома».
Монетизация: реклама Яндекс РСЯ + партнёрки маркетплейсов (Ozon/WB через Такпродам —
тот же аккаунт, что и для растений, нужно только добавить вторую площадку).

## 2. Текущий статус

**Ниша выбрана, движок развёрнут локально. Домен ещё не куплен, GitHub-репозиторий
ещё не создан, сайт нигде не опубликован.**

- ✅ Ниша: **домашние животные (кошки/собаки)** — выбрана среди 4 вариантов
  (второй вариант «Ремонт и дом» отложен на потом).
- ✅ Рабочее название: **«Лапки Дома»**, домен-кандидат `lapki-doma.ru`
  (по аналогии с «Расти Дома» — та же формула «слово + дома»).
  ⚠️ **Домен ещё не куплен.** Проверено 27.07 через whois Рег.ру — **свободен, 169 ₽**.
  Пошаговый чек-лист покупки, создания репозитория и привязки домена к GitHub Pages —
  в `купить домен, создать репозиторий,\Чек-лист — домен и репозиторий.md`.
- ✅ Движок скопирован и адаптирован из `zeleny-dom` (см. раздел 3 — что изменено).
- ✅ **Свой дизайн (27.07):** стиль Grainy Warm, главная построена вокруг проблемы
  читателя, шаблон статьи получил блоки для тревожного читателя (`reassurance`,
  `tldr`, `redFlags`), юридический слой на месте. Подробности — раздел 8.
- ✅ **Помощники (27.07):** агенты `lapki-doma-writer`, `lapki-doma-seo`,
  `lapki-doma-smm` в `C:\Users\Admin\.claude\agents\`.
- ✅ **Первые статьи (27.07):** 6 статей опубликованы (`draft: false`), сборка
  проходит без ошибок. Из них 5 — правовой блок про содержание и выгул (поле
  `legal: true`), см. журнал изменений, раздел 7.
- ⏳ Ещё не сделано: git-репозиторий, домен, любые Яндекс-сервисы (Метрика/РСЯ/
  Вебмастер), вторая площадка на Такпродам, ещё статьи (не про право), фото/обложки
  для реальных статей (пока SVG-заглушки).

## 3. Ключевые решения и договорённости

| Решение | Причина | Дата |
|---|---|---|
| Ниша — домашние животные | Вечный спрос, сильная партнёрка (зоотовары), эмоциональная аудитория, отлично заходит в Pinterest/VK | 26.07 |
| Название «Лапки Дома» | По аналогии с «Расти Дома» — узнаваемая формула бренда в портфеле | 26.07 |
| Категории: `koshki`, `sobaki`, `zdorovie-i-povedenie`, `sovety` | Параллель структуре «Расти Дома» (2 категории по типу + 1 проблемы + 1 общие советы) | 26.07 |
| Добавлена категория **`gryzuny`** (хомяки, морские свинки, кролики, крысы, шиншиллы) | Хомяки — большой спрос при слабых конкурентах; широкая категория даёт запас тем на годы вместо 15–25 статей про одних хомяков. Слоган сайта расширен до «домашних питомцев» | 27.07 |
| Правовые статьи (выгул, содержание, ответственность) — с отдельной оговоркой | Законы меняются, регионы вправе требовать строже; флаг `legal: true` подставляет юридический дисклеймер вместо ветеринарного | 27.07 |
| Лунный календарь и садовые скрипты — не переносим | Специфика растений, не относится к питомцам | 26.07 |
| ~~Дизайн (палитра/шрифты) — тот же, что у «Расти Дома»~~ **ОТМЕНЕНО 27.07** | Два сайта в портфеле выглядели бы близнецами | 26.07 |
| Свой стиль — **Grainy Warm** (тёплый персиково-кремовый, зерно, сериф Vollkorn) | Ниша эмоциональная; тёплый «домашний» вид отличает от мокко-бежевых «Расти Дома». Оранжевый приглушён до терракота `#d85a2e` — чистый `#ff6a3d` утомляет на длинных статьях | 27.07 |
| Заголовки — **Vollkorn**, не Fraunces и не DM Serif Display | У Fraunces и DM Serif Display **нет кириллицы** — русские заголовки падали на системный Georgia. Проверено запросом к Google Fonts | 27.07 |
| Главная строится вокруг проблемы читателя, а не вокруг рассказа о сайте | Человек приходит из поиска с бедой («кошка не ест»); чипы-ситуации ведут прямо к ответу | 27.07 |
| Логотип — **Playfair Display курсив**, вес 600, отдельно от шрифта заголовков | Vollkorn в шапке читался строго и «плотно»; курсив даёт изгиб и элегантность, кегль поднят с 21 до 34 px. Смена — три переменные `--font-logo*` в `global.css` + подключение шрифта в `BaseLayout` | 27.07 |
| Отказ от рецептов и личных финансов как ниш | Рецепты — забиты федеральными порталами сильнее, чем садоводство; финансы — YMYL-риск | 26.07 |

## 4. Важные ограничения и вводные

- **Аккаунты и деньги — только владелец.** Домен, GitHub-репозиторий (создаётся
  вручную на github.com — `gh` CLI недоступен в среде), Яндекс-сервисы, почта —
  всё заводит владелец, я веду по шагам.
- **Контент про здоровье животных — не YMYL в чистом виде, но близко.** Никаких
  категоричных диагнозов и схем лечения в статьях — только «частые причины» +
  явная рекомендация к ветеринару при серьёзных симптомах. См. CLAUDE.md.
- **Бюджет** — тот же принцип: 0 ₽ сверх домена (~169 ₽/год на reg.ru).
- **Кириллица в путях ломает Bash** — использовать PowerShell для команд,
  Read/Edit/Write — с абсолютным путём.

## 5. Что изменено при копировании движка из «Расти Дома»

Полностью общее, скопировано без изменений: `astro.config.mjs`, `tsconfig.json`,
`.env.example`, `.gitignore`, `content.config.ts` (кроме enum категорий),
`src/lib/related.mjs`, `src/lib/urls.mjs`, `src/layouts/ArticleLayout.astro`,
`src/components/AdSlot.astro`, `AffiliateCard.astro`, `src/pages/404.astro`,
`index.astro`, `robots.txt.ts`, `rss.xml.js`, `[category].astro`, `[category]/[slug].astro`,
`src/styles/global.css`, `scripts/lib.mjs`, `new-article.mjs`, `generate-articles.mjs`,
`.github/workflows/deploy.yml`, `partnyorskaya-programma.astro`, `politika-konfidencialnosti.astro`.

Адаптировано под тему питомцев:
- `src/site.config.mjs` — новый бренд, категории, пустые ID монетизации/аналитики
  (новые регистрации нужны), почта-заглушка `lapkidoma@yandex.com` (ещё не создана).
- `src/content.config.ts` — enum категорий: `koshki`/`sobaki`/`zdorovie-i-povedenie`/`sovety`.
- `src/layouts/BaseLayout.astro` — убраны мета-теги подтверждения владения сайтом
  (были специфичны для rasti-doma.ru — Admitad/Такпродам, для нового домена нужны свои).
- `src/components/Header.astro` — эмодзи 🌿→🐾, убрана ссылка на лунный календарь.
- `src/components/Footer.astro` — убрано слово ePN (было для rasti-doma.ru), убрана
  ссылка на лунный календарь.
- `src/components/ArticleCard.astro` — эмодзи-заглушки под новые категории
  (⚠️ важно: **не 🩺** — рисуется квадратом на Windows 10, стоит `⚕️`).
- `scripts/generate-covers.mjs` — архетипы иллюстраций переписаны: кошка/собака/
  универсальная лапа (было: горшок/лист/суккулент и т.п.).
- `scripts/fetch-photos.mjs` — карта `PHOTO_QUERIES` очищена (была под растения),
  заполнится по мере написания статей; сама логика (Wikimedia, фильтр архивных
  фото) переиспользована без изменений. User-Agent в скрипте пока со старым доменом
  — поправить, когда появится реальный домен.
- `public/favicon.svg` — новая иконка (лапа вместо листа), та же палитра.
- Статические страницы `o-saite.astro`, `kontakty.astro` — переписан текст под
  тематику питомцев, сохранена честная «мы»-подача без личных заявлений об опыте
  (тот же урок, что применили к «Расти Дома» 26.07 — см. её PROJECT.md).
- **Не перенесено вообще:** `public/lunagarden.html`, `src/pages/lunnyj-kalendar.astro`
  (специфика растений), `data/topics.json` (нужен новый список тем про питомцев),
  `README.md`, `.github/workflows/generate.yml` (нужно переименовать бота).

## 6. Следующие шаги

1. **Подтвердить и купить домен** `lapki-doma.ru` на reg.ru (или другое имя, если
   передумаем) — делает владелец.
2. **Создать пустой репозиторий на GitHub** (например `Poli6976/lapki-doma`,
   публичный — для бесплатного GitHub Pages) — делает владелец через сайт github.com,
   `gh` CLI недоступен в этой среде. После создания — я подключу и запушу код.
3. **Написать статьи** — движок и шаблон готовы, дело за контентом. Работает агент
   `lapki-doma-writer` (тон и ограничения уже зашиты внутрь), темы подбирает
   `lapki-doma-seo`. Новые поля frontmatter — `reassurance`, `tldr`, `redFlags` —
   образец в статье `kak-priuchit-kotenka-k-lotku.md`.
4. ✅ ~~Дописать оставшиеся файлы~~ — **сделано** (проверено 27.07): `README.md`
   написан, бот в `.github/workflows/generate.yml` переименован в `lapki-doma-bot`,
   User-Agent в `fetch-photos.mjs` указывает на `lapki-doma.ru`, `data/topics.json`
   на месте.
5. **Как только домен и репозиторий готовы** — повторить путь «Расти Дома»:
   CNAME → GitHub Pages → SSL → Яндекс.Вебмастер → Яндекс.Метрика → РСЯ.
   ⚠️ Заранее непонятно, будет ли Cloudflare/pages.dev блокировка — **сразу
   пробовать GitHub Pages**, без промежуточного шага через Cloudflare, раз мы уже
   знаем, что для РФ-аудитории это тупиковый путь.
5. **Написать первую пачку статей** (5–10) под сверхузкие запросы — та же стратегия,
   что дала лучший результат на «Расти Дома»: не «уход за кошкой», а «кошка не ест
   после переезда», не «дрессировка собаки», а «щенок скулит по ночам первую неделю».
6. **Такпродам — вторая площадка**, тот же аккаунт: добавить `lapki-doma.ru`,
   дождаться проверки, найти зоотовары в каталоге.

## 7. Журнал изменений

- **2026-07-26** — проект создан копированием движка «Расти Дома»: папка, конфиг,
  категории (кошки/собаки/здоровье и поведение/советы), компоненты, генератор
  SVG-обложек с новыми архетипами (кот/собака/лапа), статические страницы.
  Домен и репозиторий ещё не заведены.
- **2026-07-27** — у сайта появилось своё лицо. Стиль Grainy Warm, главная через
  проблему читателя, психологические блоки в статьях, отказоустойчивое движение,
  три агента-помощника. Подробности — раздел 8.
- **2026-07-27** — написаны первые 5 статей правового блока «что можно, что
  нельзя»: выгул собаки (ст. 13 ФЗ-498), потенциально опасные породы (ПП № 974),
  собака в многоквартирном доме, ответственность за вред от питомца, кошка и
  закон. Нормы взяты строго из проверенного брифа (КонсультантПлюс/ГАРАНТ), без
  добавлений по памяти — региональные правила и суммы штрафов не выдуманы там,
  где их не было в брифе. Обложки сгенерированы скриптом, сборка проходит без
  ошибок, блок «Читайте также» связал все 5 статей между собой.
- **2026-07-27** — написаны первые 5 статей категории `gryzuny` про хомяков:
  укусы при приручении, ночной шум, выбор клетки, кормление (можно/нельзя),
  сравнение хомяка/морской свинки/крысы для семьи с ребёнком. Везде
  `reassurance`/`tldr`, в первых четырёх — `redFlags` с упоминанием ратолога.
  Обложки сгенерированы (архетип грызуна), сборка проходит без ошибок, «Читайте
  также» связал статьи между собой через общие keywords.
- **2026-07-27** — наполнена категория `zdorovie-i-povedenie` (была пустой,
  хотя чипы-ситуации с главной уже вели туда): 6 статей — «Кошка не ест»,
  «Собака отказывается от еды», «Собака чешется, а блох нет», «Рвота у
  кошки», «Собака боится грозы и салютов», «Питомец сильно линяет». Везде
  `reassurance`/`tldr`/`redFlags`, без диагнозов и препаратов, с явным
  предупреждением про опасность человеческих обезболивающих в статьях про
  отказ от еды и рвоту. Обложки сгенерированы, сборка проходит без ошибок,
  чипы «Не ест», «Чешется», «Боится грозы и салютов», «Рвота», «Сильно
  линяет» на главной теперь ведут на конкретные статьи, а не на категорию.
- **2026-07-27** — подготовка к публикации. Домен `lapki-doma.ru` проверен —
  свободен, 169 ₽ на reg.ru. Рабочие папки сессий (`Дизайн/`, `купить домен,
  создать репозиторий,/`) добавлены в `.gitignore`, чтобы личные материалы и
  заметки не уехали в публичный репозиторий. Заведён чек-лист покупки домена,
  создания репозитория и привязки к GitHub Pages (в папке «купить домен, создать
  репозиторий,»). Покупка и создание репозитория — за владельцем.

## 8. Проектирование 27.07.2026 — что и почему сделано

Основа — четыре материала владельца: дизайн-скилл `modern-design-2026`, гайд «10
трендовых стилей 2026», скилл психолога и скилл юриста.

**Дизайн (`src/styles/global.css`, `src/layouts/BaseLayout.astro`)**
- Стиль **Grainy Warm**: тёплый фон `#fbf3ea` с персиковым подсветом вверху,
  плёночное зерно поверх страницы, терракотовый акцент `#d85a2e`.
- Зерно — `opacity: .12` вместо `.35` из гайда: плотное зерно мешает читать длинные
  статьи.
- Заголовки — **Vollkorn**. Fraunces из оригинального стиля и прежний DM Serif
  Display **не имеют кириллицы**, русские заголовки падали на Georgia.
- `--text-soft` затемнён до `#786052`: вариант из гайда давал контраст 4.09 — ниже
  нормы WCAG AA. Стало 5.32.
- Шкала отступов `--sp-1…--sp-9` с большими скачками между секциями.
- Движение построено на **переходах, а не на keyframes с fill-mode**: конечный вид
  задаёт класс, поэтому незапустившаяся анимация не может «съесть» контент.
  Плюс страховочные таймеры, которые показывают всё принудительно.

**Главная (`src/pages/index.astro`)**
- Вход через вопрос «Что случилось у питомца?» и чипы-ситуации. Чип сам находит
  статью по ключевым словам, а пока её нет — ведёт на категорию.
- Бенто-сетка категорий с намеренно неравными ячейками (7 и 5 колонок из 12).
- Блок «Как мы пишем» — доверие для читателя и E-E-A-T для поиска.

**Статья (`src/layouts/ArticleLayout.astro`, новые компоненты)**
- `CalmIntro` — валидация + «что сделать в первую очередь» (принцип психолога
  «сначала присоединение, потом техника»).
- `VetAlert` — красные флаги. Полоса слева, а не красная заливка: напуганному
  человеку красный экран делает хуже.
- `VetDisclaimer` — справочный характер материалов.
- Партнёрское раскрытие **перенесено** из шапки статьи к блоку товаров: раньше
  первое, что видел встревоженный человек, — «мы получаем комиссию».
- Новые поля схемы: `reassurance`, `tldr`, `redFlags` (все опциональные).

**Юридический слой** — политика под 152-ФЗ, cookie-полоса, маркировка рекламы по
ст. 18.1 ФЗ «О рекламе» (поля `erid` и `advertiser` в товарах), сведения о владельце.
⚠️ Это заготовка, а не юридическое заключение: режим маркировки партнёрских ссылок
владельцу стоит подтвердить у практикующего юриста до подключения партнёрок.
