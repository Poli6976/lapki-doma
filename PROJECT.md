# Файл состояния проекта — «Лапки Дома»

> **Единая точка входа.** В начале новой сессии достаточно прочитать этот файл —
> пересказывать прошлую переписку не нужно. Обновлять после каждого значимого шага.
>
> 📁 **Папка проекта:** `D:\Рабочий стол\МОИ ПРОЕКТЫ\lapki-doma`
>
> 🔗 **Второй проект в портфеле пользователя.** Первый и уже работающий —
> «Расти Дома» (растения), папка `D:\Рабочий стол\МОИ ПРОЕКТЫ\zeleny-dom`,
> сайт rasti-doma.ru. «Лапки Дома» построены копированием и адаптацией того же
> движка (Astro → GitHub Pages → Яндекс РСЯ → партнёрки маркетплейсов). При
> сомнениях в устройстве кода можно свериться с оригиналом, но НЕ смешивать
> файлы/контент двух проектов.

**Дата последнего обновления: 2026-07-26**

---

## 1. Цель проекта

Русскоязычный контентный сайт про уход за кошками и собаками — второй источник
пассивного дохода, по тому же проверенному плейбуку, что и «Расти Дома».
Монетизация: реклама Яндекс РСЯ + партнёрки маркетплейсов (Ozon/WB через Такпродам —
тот же аккаунт, что и для растений, нужно только добавить вторую площадку).

## 2. Текущий статус

**Ниша выбрана, движок развёрнут локально. Домен ещё не куплен, GitHub-репозиторий
ещё не создан, сайт нигде не опубликован.**

- ✅ Ниша: **домашние животные (кошки/собаки)** — выбрана среди 4 вариантов
  (второй вариант «Ремонт и дом» отложен на потом).
- ✅ Рабочее название: **«Лапки Дома»**, домен-кандидат `lapki-doma.ru`
  (по аналогии с «Расти Дома» — та же формула «слово + дома»).
  ⚠️ **Домен ещё не куплен** — нужно подтвердить у пользователя и купить на reg.ru.
- ✅ Движок скопирован и адаптирован из `zeleny-dom` (см. раздел 3 — что изменено).
- ⏳ Ещё не сделано: git-репозиторий, домен, любые Яндекс-сервисы (Метрика/РСЯ/
  Вебмастер), вторая площадка на Такпродам, статьи, фото/обложки для реальных статей.

## 3. Ключевые решения и договорённости

| Решение | Причина | Дата |
|---|---|---|
| Ниша — домашние животные | Вечный спрос, сильная партнёрка (зоотовары), эмоциональная аудитория, отлично заходит в Pinterest/VK | 26.07 |
| Название «Лапки Дома» | По аналогии с «Расти Дома» — узнаваемая формула бренда в портфеле | 26.07 |
| Категории: `koshki`, `sobaki`, `zdorovie-i-povedenie`, `sovety` | Параллель структуре «Расти Дома» (2 категории по типу + 1 проблемы + 1 общие советы) | 26.07 |
| Лунный календарь и садовые скрипты — не переносим | Специфика растений, не относится к питомцам | 26.07 |
| Дизайн (палитра/шрифты) — тот же, что у «Расти Дома» | Не тратить время на новую айдентику, движок уже проверен | 26.07 |
| Отказ от рецептов и личных финансов как ниш | Рецепты — забиты федеральными порталами сильнее, чем садоводство; финансы — YMYL-риск | 26.07 |

## 4. Важные ограничения и вводные

- **Аккаунты и деньги — только владелец.** Домен, GitHub-репозиторий (создаётся
  вручную на github.com — `gh` CLI недоступен в среде), Яндекс-сервисы, почта —
  всё заводит владелец, я веду по шагам.
- **Контент про здоровье животных — не YMYL в чистом виде, но близко.** Никаких
  категоричных диагнозов и схем лечения в статьях — только «частые причины» +
  явная рекомендация к ветеринару при серьёзных симптомах. См. CLAUDE.md.
- **Бюджет** — тот же принцип: 0 ₽ сверх домена (~169 ₽/год на reg.ru).
- **Кириллица в путях ломает Bash** — использовать PowerShell для команд,
  Read/Edit/Write — с абсолютным путём.

## 5. Что изменено при копировании движка из «Расти Дома»

Полностью общее, скопировано без изменений: `astro.config.mjs`, `tsconfig.json`,
`.env.example`, `.gitignore`, `content.config.ts` (кроме enum категорий),
`src/lib/related.mjs`, `src/lib/urls.mjs`, `src/layouts/ArticleLayout.astro`,
`src/components/AdSlot.astro`, `AffiliateCard.astro`, `src/pages/404.astro`,
`index.astro`, `robots.txt.ts`, `rss.xml.js`, `[category].astro`, `[category]/[slug].astro`,
`src/styles/global.css`, `scripts/lib.mjs`, `new-article.mjs`, `generate-articles.mjs`,
`.github/workflows/deploy.yml`, `partnyorskaya-programma.astro`, `politika-konfidencialnosti.astro`.

Адаптировано под тему питомцев:
- `src/site.config.mjs` — новый бренд, категории, пустые ID монетизации/аналитики
  (новые регистрации нужны), почта-заглушка `lapkidoma@yandex.com` (ещё не создана).
- `src/content.config.ts` — enum категорий: `koshki`/`sobaki`/`zdorovie-i-povedenie`/`sovety`.
- `src/layouts/BaseLayout.astro` — убраны мета-теги подтверждения владения сайтом
  (были специфичны для rasti-doma.ru — Admitad/Такпродам, для нового домена нужны свои).
- `src/components/Header.astro` — эмодзи 🌿→🐾, убрана ссылка на лунный календарь.
- `src/components/Footer.astro` — убрано слово ePN (было для rasti-doma.ru), убрана
  ссылка на лунный календарь.
- `src/components/ArticleCard.astro` — эмодзи-заглушки под новые категории
  (⚠️ важно: **не 🩺** — рисуется квадратом на Windows 10, стоит `⚕️`).
- `scripts/generate-covers.mjs` — архетипы иллюстраций переписаны: кошка/собака/
  универсальная лапа (было: горшок/лист/суккулент и т.п.).
- `scripts/fetch-photos.mjs` — карта `PHOTO_QUERIES` очищена (была под растения),
  заполнится по мере написания статей; сама логика (Wikimedia, фильтр архивных
  фото) переиспользована без изменений. User-Agent в скрипте пока со старым доменом
  — поправить, когда появится реальный домен.
- `public/favicon.svg` — новая иконка (лапа вместо листа), та же палитра.
- Статические страницы `o-saite.astro`, `kontakty.astro` — переписан текст под
  тематику питомцев, сохранена честная «мы»-подача без личных заявлений об опыте
  (тот же урок, что применили к «Расти Дома» 26.07 — см. её PROJECT.md).
- **Не перенесено вообще:** `public/lunagarden.html`, `src/pages/lunnyj-kalendar.astro`
  (специфика растений), `data/topics.json` (нужен новый список тем про питомцев),
  `README.md`, `.github/workflows/generate.yml` (нужно переименовать бота).

## 6. Следующие шаги

1. **Подтвердить и купить домен** `lapki-doma.ru` на reg.ru (или другое имя, если
   передумаем) — делает владелец.
2. **Создать пустой репозиторий на GitHub** (например `Poli6976/lapki-doma`,
   публичный — для бесплатного GitHub Pages) — делает владелец через сайт github.com,
   `gh` CLI недоступен в этой среде. После создания — я подключу и запушу код.
3. **Дописать оставшиеся файлы** (доделать в этой же сессии): `README.md`,
   переименовать бота в `.github/workflows/generate.yml`, обновить User-Agent
   в `fetch-photos.mjs` под реальный домен, создать стартовый `data/topics.json`
   с темами про кошек/собак.
4. **Как только домен и репозиторий готовы** — повторить путь «Расти Дома»:
   CNAME → GitHub Pages → SSL → Яндекс.Вебмастер → Яндекс.Метрика → РСЯ.
   ⚠️ Заранее непонятно, будет ли Cloudflare/pages.dev блокировка — **сразу
   пробовать GitHub Pages**, без промежуточного шага через Cloudflare, раз мы уже
   знаем, что для РФ-аудитории это тупиковый путь.
5. **Написать первую пачку статей** (5–10) под сверхузкие запросы — та же стратегия,
   что дала лучший результат на «Расти Дома»: не «уход за кошкой», а «кошка не ест
   после переезда», не «дрессировка собаки», а «щенок скулит по ночам первую неделю».
6. **Такпродам — вторая площадка**, тот же аккаунт: добавить `lapki-doma.ru`,
   дождаться проверки, найти зоотовары в каталоге.

## 7. Журнал изменений

- **2026-07-26** — проект создан копированием движка «Расти Дома»: папка, конфиг,
  категории (кошки/собаки/здоровье и поведение/советы), компоненты, генератор
  SVG-обложек с новыми архетипами (кот/собака/лапа), статические страницы.
  Домен и репозиторий ещё не заведены.
